<%= form_with(model: tpost) do |form| %>
  <% if tpost.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tpost.errors.count, "error") %> prohibited this tpost from being saved:</h2>

      <ul>
        <% tpost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
